{
  "name": "workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd packages/ly && pnpm clean && pnpm build",
    "example:basic": "cd examples/basic && pnpm dev",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.2.3"
  }
}
